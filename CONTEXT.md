# CONTEXT — พจนานุกรมของ api-vault

> นิยามคำเท่านั้น ไม่มีรายละเอียด implement · การตัดสินใจอยู่ที่ [`docs/adr/`](docs/adr/)

| คำ | แปลว่า | ไม่ใช่ |
|---|---|---|
| **Entry** | API หนึ่งตัวในแคตตาล็อก (ชั้น 1) — ชื่อ คำอธิบาย หมวด auth/HTTPS/CORS ลิงก์ และแท็กตัดสิน | ไม่ใช่ key · ไม่ใช่ endpoint ย่อยของ API |
| **Catalogue** | ชั้น 1: Entry ทั้งหมด — 1,873 รายการตั้งต้นจาก `public-apis` (MIT, ให้เครดิต) + ที่เจ้าของเพิ่มเอง | ไม่รวม API งาน/ภายในองค์กร |
| **Key** | ความลับหนึ่งชิ้นที่เจ้าของถือไว้สำหรับ Entry หนึ่งตัว (API key, token) เก็บแบบเข้ารหัสเสมอ | ไม่ใช่ master key |
| **Vault** | ชั้น 2: ที่เก็บ Key ทั้งหมดแบบเข้ารหัส + audit log | ไม่ใช่ gateway — Vault ไม่ยิง API แทนใคร (ADR-0001) |
| **Master key** | กุญแจที่ห่อ DEK ของทุก Key อยู่ใน env ของ Vercel เท่านั้น | ไม่เคยอยู่ใน DB |
| **DEK** | data encryption key สุ่มใหม่ต่อ Key หนึ่งชิ้น เก็บในรูปที่ถูก master key ห่อแล้ว | |
| **Reveal** | การถอดรหัส Key ให้เจ้าของเห็น/ได้ค่า — ต้องยืนยันตัวใหม่ และถูกบันทึกทุกครั้ง | การ list ว่ามี Key อะไรบ้าง ไม่ใช่ Reveal |
| **Pull** | `vault pull <project>` — CLI ดึง Key ของโปรเจกต์หนึ่งลง `.env` (นับเป็น Reveal) | |
| **Ranker** | ตัวที่รับคำค้น (ไทยหรืออังกฤษ) แล้วคืน Entry เรียงตามความน่าจะตรง + ความมั่นใจ หรือบอกตรง ๆ ว่า "ไม่มีที่ตรง" | ไม่ใช่ตัวสร้างข้อความ |
| **No match** | ผลของ Ranker ที่บอกว่าไม่มี Entry ไหนตรงพอ — เป็นคำตอบที่ถูกต้องได้ ไม่ใช่ error | |
| **Tag** | ค่าที่ตัดสินให้ Entry ตอนนำเข้า: ใช้เชิงพาณิชย์ได้ไหม · ต้องสมัครไหม · เกี่ยวกับไทยไหม · คุณภาพคำอธิบาย | auth/HTTPS/CORS ไม่ใช่ Tag — มาจากข้อมูลต้นทาง |
| **Demo** | หน้าเดโมสาธารณะที่โชว์ผลค้น 5–8 ข้อที่คำนวณไว้แล้ว (มีข้อ "No match" หนึ่งข้อ) | ไม่ยิง Ranker สด ไม่มี Key ใน build (ADR-0004) |
| **Version** | เวอร์ชันของ**แอป** (semver + `CHANGELOG.md` + หน้า `/about`) | ไม่ใช่ประวัติการแก้ของ Entry (อันนั้นคือ V1.1) |
| **Jev** | โมเดลของ TypeSafe ที่ตอบเป็นการตัดสินแบบมีชนิด (Choice/Nouls/Score) — Ranker ตัวแรกที่ตั้งใจใช้ | |
