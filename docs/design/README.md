# design

`api-vault.drawio` — ดีไซน์ทั้งระบบ 10 หน้า (ภาพรวม · ER · ค้นหา · นำเข้า · Vault · Auth · CLI · Deploy · Roadmap · raw)

**แก้ใน draw.io โดยตรง** — generator ที่ใช้สร้างครั้งแรกไม่ได้เก็บไว้ ห้ามสร้างไฟล์ใหม่ทับ (งานที่วาดมือจะหาย)
เพิ่มหน้าใหม่ = แทรก `<diagram>` ก่อนหน้า "99 raw" · หน้า **99 raw (ของผู้ใช้)** มีไว้ร่าง feedback แล้วบอก "ดูหน้า raw"

ตรวจภาพ: `"/Applications/draw.io.app/Contents/MacOS/draw.io" -x -f png -p <หน้า เริ่ม 1> -o page.png api-vault.drawio`
